/**
 * Página Como Funciona (placeholder do scaffold — implementação completa
 * conforme como-funciona.md).
 */
export default function ComoFunciona() {
  return (
    <section className="container-site py-[clamp(5rem,12vh,9rem)]">
      <p className="eyebrow mb-4 text-teal">Como funciona</p>
      <h1 className="font-display text-[clamp(2.8rem,7vw,6rem)] font-medium leading-none text-mist">
        100% digital, <em className="text-iris-gradient italic">do jeito do Olho.</em>
      </h1>
    </section>
  );
}
