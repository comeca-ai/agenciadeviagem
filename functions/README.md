# Legacy Pages Functions

These handlers are kept as a compatibility reference during the Pages → Worker migration. Production requests are routed by `worker/index.js` to the ported modules under `worker/api/`. Remove this directory in a follow-up only after the Worker cutover has been validated.
